import * as React from 'preact';

export const Main = () => (
    <div>
        <LoginForm></LoginForm>
    </div>
);

export const LoginForm = () => (
    <div>
        <input/>
        <input/>
        <button>Login</button>
    </div>
);